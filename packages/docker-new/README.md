# ALPHA: Next gen docker implementation

Ideally, I'd like to use Docker's go library, compiled to WASI, to interact with Docker.

Currently, not all the syscalls required are supported, so this is on hold.
