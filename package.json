{
  "name": "root",
  "packageManager": "yarn@3.4.1",
  "scripts": {
    "prebuild": "yarn install",
    "build": "lerna run build",
    "pretest": "lerna run build",
    "test": "lerna run test",
    "start": "jest -c e2e/out/jest.config.js"
  },
  "workspaces": [
    "packages/*",
    "e2e"
  ],
  "dependencies": {
    "jest": "^29.4.3"
  },
  "devDependencies": {
    "lerna": "^6.5.1",
    "typescript": "^4.9.5"
  },
  "private": true
}
